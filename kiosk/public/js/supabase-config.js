/* Public Supabase config for the browser.
 *
 * SUPABASE_ANON_KEY is designed to be public (RLS protects data).
 * Never put SERVICE_ROLE or Google Client Secret here.
 *
 * Fill these from Supabase → Project Settings → API.
 * After editing, hard-refresh the site (or bump ?v= on script tags).
 */
export const SUPABASE_URL = 'https://YOUR_PROJECT_REF.supabase.co';
export const SUPABASE_ANON_KEY = 'YOUR_SUPABASE_ANON_KEY';
